# Last Light predecessor handoff

This directory contains prepared predecessor material. It is excluded from the empty
Last Light seed and from ordinary creator context. The current native compiler locally
solved and compiled it to qualify Forge's ABI 2 worker; that does not create a creator
proposal, scene review, upload authorization, Roblox receipt, native inspection,
Studio mutation, persistence result, Play observation, or product proof.

## Current local identities

- Scene revision 14:
  `f29ea71de5187c5124bdde75dbc0a07d225060f996b820050dcbb4718e1c908b`.
- Manifest:
  `d6dc495e6735b433e954f4020a975f26e8edfe16afd5024651de785d9d755f4e`.
- Manifest artifact:
  `/Users/mawawdi/Desktop/forge/.forge/last-light/qualification-abi2-attempt-12/binary-artifacts/d4f6aedcdde41175537244080b850d58f2889d7703c500a5e90c8cd90019632f.bin`.
- Human-readable review packet:
  `/Users/mawawdi/Desktop/forge/.forge/last-light/qualification-abi2-attempt-12/review`.
- Installation qualification:
  `/Users/mawawdi/Desktop/forge/.forge/blender/installation-qualification-v2.json`,
  hash `08409634ba8cee7ddc424633ed1ff23429ff2cc03c17a881c943b35ddb5162c8`.
- Installed connector:
  `/Users/mawawdi/Documents/Roblox/Plugins/ForgeStudioPlugin.rbxmx`.

The repository-root DMG is mounted read-only at `/Volumes/Blender`; Blender was not
installed. Recheck the exact installation with:

```sh
cd /Users/mawawdi/Desktop/forge
shasum -a 256 blender-5.2.1-macos-arm64.dmg
codesign --verify --deep --strict /Volumes/Blender/Blender.app
spctl --assess --type execute --verbose=4 /Volumes/Blender/Blender.app
node dist/packages/cli/src/index.js visual blender-status \
  --installation /Users/mawawdi/Desktop/forge/.forge/blender/installation-qualification-v2.json
```

The DMG hash must be
`6409e21de80994db5f4c4a34486b6fd43cea21085b912f7491c53e923acb65a3`.

## Current handoff: local visual review only

Review the four exact retained renders:

```sh
open /Users/mawawdi/Desktop/forge/.forge/last-light/qualification-abi2-attempt-12/review/renders/opening-view.png
open /Users/mawawdi/Desktop/forge/.forge/last-light/qualification-abi2-attempt-12/review/renders/reactor-shuttle-approach.png
open /Users/mawawdi/Desktop/forge/.forge/last-light/qualification-abi2-attempt-12/review/renders/warning-interaction.png
open /Users/mawawdi/Desktop/forge/.forge/last-light/qualification-abi2-attempt-12/review/renders/reactor-restored.png
```

`LOCAL-REVIEW.json` lists all 21 output paths, byte counts, and hashes. A creator
decision for an ordinary product run must bind the proposal and generated bundle from
that run. Do not upload this predecessor packet as the Last Light product proof.

## Native sequence after an ordinary approved bundle exists

Forge supports OAuth, API-key, and exact manual-import modes. Credentials stay in the
macOS Keychain helper and never enter scene JSON, model context, browser storage,
arguments, logs, or retained artifacts. After proposal acceptance and exact bundle
review, authorize the creator/group, target project and universe, credential
capability, and every export hash. The Open Cloud path retains each intent before one
POST and never resubmits an unknown outcome. The manual packet uses the same verified
GLB bytes and does not manufacture a receipt.

Only after ownership, moderation, dependency access, exact-version delivery, and the
detached conformance profile are eligible:

1. Build the connector and start the bridge:

   ```sh
   cd /Users/mawawdi/Desktop/forge
   npm run plugin:build
   node dist/packages/cli/src/index.js studio bridge
   ```

2. Open the newly generated product-proof place in Roblox Studio. In **Plugins**, open
   **Forge Studio**, enable local HTTP and script injection if Studio asks, and verify
   the exact project revision and connector build shown by Forge.
3. In Forge, run **Inspect Approved Scene Assets** for the exact current challenge.
   The plugin loads each asset detached with `AssetService:LoadAssetAsync`, validates
   the package envelope and complete descendant observations, removes the permitted
   package link, and destroys detached instances. Do not prepare a mutation if this is
   incomplete or rejected.
4. Review the exact native plan, then explicitly choose **Prepare**. Confirm the closed
   operation inventory and choose **Apply** once. Network loading has already ended;
   the transaction creates stable wrappers, visual children, collision proxies,
   anchors, sockets, effects, UI, and reviewed sources.
5. Save to the handoff's exact place path, close Studio completely, reopen that saved
   file, reopen Forge Studio, and submit a fresh authoritative capture. Reloading the
   plugin alone is not reopen evidence.
6. Choose **Test → Play** (F5). Complete and retain ten win/loss/replay cycles, including
   timeout and character death. Record real versus emulated input device, viewport,
   graphics settings, diagnostics, performance observations, and creator judgments
   separately.
7. Review the named scene and phone/desktop UI views. Route an observed defect through
   a selective repair proposal, changed-artifact review, fresh asset authority where
   needed, detached replacement inspection, and one creator-initiated Apply.

The product proof remains `incomplete` until these creator-owned results exist.
