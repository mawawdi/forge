# Orbital Freight Airlock creator-session fixture

This is a solution-free Studio seed for a single interconnected creator-session proof. Build it to a temporary place, open that place in Studio, and do not keep Rojo connected: Studio is the creator session's sole writer.

The seed contains the named physical assembly, an empty replicated networking folder, and an empty HUD panel because every approved create must have a parent that existed in the initial Studio state. It deliberately contains no prompts, remotes, scripts, UI controls, evaluator material, or prepared behavior.

Use this exact prompt:

> Turn Workspace/OrbitalFreightAirlock into a complete server-authoritative orbital freight airlock. Add ProximityPrompts labeled “Enable Power”, “Open Coolant”, “Scan Cargo”, and “Emergency Reset” to PowerConsole, CoolantValve, CargoScanner, and EmergencyConsole respectively. Populate ReplicatedStorage/AirlockSystem with Command, a RemoteEvent for bounded client requests; StateChanged, a RemoteEvent for complete server-to-client state snapshots; GetState, a RemoteFunction for initial synchronization; and Protocol, a ModuleScript containing the shared phase names, action names, and snapshot validation. Create the authoritative implementation in ServerScriptService/AirlockServer and the client implementation in StarterPlayer/StarterPlayerScripts/AirlockClient. The server owns the state machine LOCKED → READY → SEALING → EQUALIZING → CLEAR, with ABORTING → LOCKED available from every non-locked phase. Start with OuterDoor open at its existing position, InnerDoor closed at its existing position, pressure 0, and all prerequisites false. Power, coolant, and cargo scanning may be completed in any order while locked; READY requires all three. Dispatch before READY must be rejected without movement or pressure change. A valid dispatch moves OuterDoor straight down 8 studs and waits until it is closed, raises authoritative pressure monotonically from 0 to 100 over approximately four seconds, then moves InnerDoor straight up 8 studs and enters CLEAR. The anchored doors must never be open together. Emergency reset must invalidate stale asynchronous work, close InnerDoor, return pressure to 0, reopen OuterDoor only after InnerDoor is closed, clear all prerequisites, and finish in LOCKED. Store phase, pressure, prerequisite booleans, and a monotonically increasing sequence as server-owned attributes on Workspace/OrbitalFreightAirlock. Broadcast complete sanitized snapshots; the client ignores any snapshot whose sequence is not newer than the last accepted sequence. Build a technical HUD inside StarterGui/AirlockHUD/Panel showing phase, pressure, the three prerequisites, the most recent rejection, a Dispatch button, and an Abort/Reset button. The HUD requests actions and renders snapshots but never moves doors or decides authoritative state. Client Command payloads contain only an allowlisted action and a bounded request ID. The server rejects unknown fields/actions, malformed or oversized values, repeated request IDs, illegal phases, missing or dead characters, and requests exceeding a per-player rate limit; it never accepts a client-provided instance, path, position, pressure, phase, prerequisite, or target player. For every prompt activation, independently validate on the server that the prompt is enabled, the player has a living character within 12 studs of its console, the phase permits the action, and the rate limit permits it. Use server-created TweenService animations and make emergency cancellation generation-safe. Give the existing Power, Pressure, and Clearance indicator parts distinct inactive, ready, transitional, and fault appearances using locally created lights or particle effects without uploaded asset IDs. Do not use DataStoreService, external assets, arbitrary require calls, or unbounded loops. Preserve Workspace/PreservedScenery exactly and preserve every seeded airlock part's name, size, and initial transform except approved runtime movement of OuterDoor and InnerDoor. The visible plan must cover every required instance/source artifact, Luau syntax, client/server authority, remote validation and rate limiting, initial synchronization and stale-snapshot rejection, legal and illegal transitions, the never-both-open invariant, emergency cancellation, bounded diagnostics, preservation, and creator review of the HUD, indicators, denied dispatch, complete dispatch sequence, and recovery. Keep machine claims limited to evidence Forge can actually observe; unsupported causality, UI behavior, visual quality, and interaction behavior remain explicit creator-review items.

The intended creator-run sequence is:

1. Press **Dispatch** immediately and confirm rejection with no door movement.
2. Trigger power, coolant, and cargo scan in any order and confirm the HUD reaches `READY`.
3. Press **Dispatch** and observe outer close → pressure rise → inner open.
4. Trigger **Emergency Reset** and confirm inner close → pressure zero → outer reopen → `LOCKED`.
5. Record only what was actually observed in the required final report.

The interaction sequence, HUD rendering, causal ordering, and visual quality remain creator-authority evidence unless the approved runtime projection covers a specific fact directly.
