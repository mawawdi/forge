{
	"name": "forge",
	"private": true,
	"type": "module",
	"bin": {
		"forge": "bin/forge.js"
	},
	"scripts": {
		"roblox-api-catalog:refresh": "node scripts/generate-roblox-api-catalog.mjs --refresh --clone",
		"roblox-api-catalog:check": "node scripts/generate-roblox-api-catalog.mjs --check",
		"studio-evidence:generate": "node scripts/generate-studio-evidence.mjs",
		"studio-evidence:check": "node scripts/generate-studio-evidence.mjs --check",
		"build": "rm -rf dist && tsc -p tsconfig.json",
		"test": "npm run roblox-api-catalog:check && npm run studio-evidence:check && npm run build && npm run dashboard:build && node --test dist/test/*.test.js && npm run dashboard:test && npm run plugin:test && npm run formal:check",
		"dashboard:build": "npm --prefix dashboard run build",
		"dashboard:dev": "npm --prefix dashboard run dev",
		"dashboard:test": "npm --prefix dashboard test",
		"plugin:test": "find plugin/src -name '*.luau' -print0 | xargs -0 luau-compile --only-parse && luau-lsp analyze --definitions packages/luau-toolchain/roblox/globalTypes.d.luau plugin/src/Forge/RuntimeCapabilityEncoding.luau plugin/src/Forge/Hash.luau && lune run scripts/test-plugin-modules.luau",
		"plugin:build": "rojo build plugin/default.project.json -o plugin/ForgeStudioPlugin.rbxmx",
		"formal:setup": "node scripts/setup-tla.mjs",
		"formal:check": "node scripts/check-tla.mjs",
		"verify": "npm run build --silent && node dist/packages/cli/src/index.js verify",
		"build:all": "npm run build && npm run dashboard:build && npm run plugin:build"
	},
	"devDependencies": {
		"@types/node": "^22.20.1",
		"typescript": "^5.9.3",
		"yaml": "2.9.0"
	},
	"dependencies": {
		"@openrouter/ai-sdk-provider": "3.0.0",
		"ai": "7.0.85",
		"zod": "4.5.4"
	}
}
