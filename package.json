{
  "name": "lemonade-forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "forge": "bin/forge.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js && npm run plugin:test",
    "plugin:test": "find plugin/src -name '*.luau' -print0 | xargs -0 luau-compile --only-parse && luau-analyze plugin/src/Forge/ObservationRevision.luau scripts/test-plugin-modules.luau && lune run scripts/test-plugin-modules.luau",
    "plugin:build": "rojo build plugin/default.project.json -o plugin/ForgeStudioPlugin.rbxmx",
    "verify": "npm run build --silent && node dist/packages/cli/src/index.js verify"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
